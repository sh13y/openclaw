services:
  - type: web
    name: openclaw
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: BSCSCAN_API_KEY
        sync: false
      - key: NODE_VERSION
        value: "20"
